# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Exportações do D1 para backup: contêm dado pessoal dos inscritos.
# O repositório é PÚBLICO — isto nunca pode ser versionado.
backups/

# Cache do Python (scripts/fotos_*.py)
__pycache__/
*.pyc

# venv do indexador de rostos (scripts/fotos_faces.py)
.venv-faces/

# modelos de rosto (baixados por scripts/fotos_faces_modelos.sh)
scripts/modelos/

# Estado local do wrangler (`pages dev`). NUNCA versionar: o R2 da sua maquina
# guarda faces/<ano>.bin, que e o banco de impressoes faciais de todo mundo que
# apareceu no evento — e este repositorio e PUBLICO.
.wrangler/

# motor wasm do onnxruntime, copiado do node_modules pelo scripts/copiar-ort-wasm.js
# (prebuild). Ver o cabeçalho do script para o porquê de não versionar.
public/ort/
